



<% if flash[:notice] %>
<div class="flash"><%= flash[:notice] %></div>
<% end %>




<img src="<%= @product.url %>" alt="@product.id"> <br>
<h4>Desription</h4>
<p><%= @product.name %></p>
<p><%= @product.price_in_cents %></p>
<p><%= @product.description %></p>


<div class="edit_delete">
<%= link_to("Edit", "/products/#{@product.id}/edit") %> <%= link_to("Delete", "/products/#{@product.id}", method: :delete, data: {confirm: "Are you sure you want to delete this picture?"})  %> </p>
</div>

<%= render 'reviews/forms_review', review:@review, product:@product %>

<div class="reviews">
  <h2>Reviews</h2>
  <%@reviews.each do |review| %>
  <uL>
  <p>Name:<%=review.name %></p>
  <p>Rating:<%=review.rating %></p>
  <p>Review:<%=review.message %></p>
  <p><%= link_to("Delete", product_review_path(@product.id, review.id), method: :delete, data: {confirm: "Are you sure you want to delete this review?"}) %></p>
  </ul>
  <% end%>
</div>
